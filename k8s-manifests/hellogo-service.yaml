apiVersion: v1
kind: Service
metadata:
  labels:
    app: hellogo
  name: hellogo
spec:
  externalTrafficPolicy: Cluster
  ports:
  - port: 8080
    targetPort: 3000
    name: http
  selector:
    app: hellogo
